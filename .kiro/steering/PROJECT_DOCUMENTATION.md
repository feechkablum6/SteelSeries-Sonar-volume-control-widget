# 📘 Проектная документация: StellGG Volume Mixer Widget

**Версия документации:** 1.0  
**Дата создания:** 2025-11-24  
**Последнее обновление:** 2025-11-24 22:56

---

## 📋 Содержание

1. [Краткое описание проекта](#краткое-описание-проекта)
2. [Цели и задачи](#цели-и-задачи)
3. [Архитектура и основные компоненты](#архитектура-и-основные-компоненты)
4. [Технологии и зависимости](#технологии-и-зависимости)
5. [Структура проекта](#структура-проекта)
6. [Код-стайл и стандарты](#код-стайл-и-стандарты)
7. [Правила ведения Changelog](#правила-ведения-changelog)
8. [Журнал ключевых изменений](#журнал-ключевых-изменений)
9. [Best Practices](#best-practices)
10. [Типовые ошибки и FAQ](#типовые-ошибки-и-faq)
11. [Развитие и roadmap](#развитие-и-roadmap)
12. [Контакты и фидбэк](#контакты-и-фидбэк)

---

## 1. Краткое описание проекта

**StellGG Volume Mixer Widget** — это desktop-виджет для **Windows**, предоставляющий пользователю визуально привлекательный интерфейс для управления громкостью различных аудиоканалов (Общая, Игра, Чат, Медиа, Микрофон).

### Основные возможности:
- ✨ **Прозрачное окно** с эффектом "запотевшего стекла" (frosted glass / glassmorphism)
- 🎚️ **5 независимых каналов** управления громкостью
- 🔇 **Кнопки мута** с визуальной индикацией
- 🎨 **Стильный modern UI** с цветными индикаторами пресетов
- 📱 **Компактный размер** (450x300px) — не загромождает экран
- 🖱️ **Drag-and-drop** перемещение окна

---

## 2. Цели и задачи

### Основная цель:
Создать **минималистичный, функциональный и визуально впечатляющий** инструмент для управления звуком в Windows, который:
- Не отвлекает от основной работы
- Быстро доступен
- Имеет премиальный внешний вид

### Задачи проекта:
1. ✅ Реализовать прозрачное окно с эффектом blur
2. ✅ Создать 5 каналов управления (Master, Game, Chat, Media, Mic)
3. ✅ Добавить интерактивные слайдеры с визуальной обратной связью
4. ✅ Реализовать функционал мута
5. 🔄 Интегрировать с Windows Audio API (будущая задача)
6. 🔄 Добавить управление приложениями по каналам (будущая задача)

---

## 3. Архитектура и основные компоненты

### 3.1. Архитектурная схема

```
┌─────────────────────────────────────────────┐
│          Electron Main Process              │
│             (main.js)                       │
│  - Создание окна BrowserWindow              │
│  - Настройка прозрачности                   │
│  - Управление жизненным циклом              │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│        Electron Renderer Process            │
│     (index.html + renderer.js)              │
│  - Отрисовка UI                             │
│  - Обработка событий (слайдеры, кнопки)     │
│  - Обновление DOM                           │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│            Styling Layer                    │
│             (styles.css)                    │
│  - Glassmorphism эффекты                    │
│  - Responsive layout                        │
│  - Анимации и transitions                   │
└─────────────────────────────────────────────┘
```

### 3.2. Основные компоненты

#### **main.js** — Main Process
**Ответственность:**
- Инициализация приложения Electron
- Создание прозрачного окна с настройками:
  - `transparent: true`
  - `frame: false` (без рамки Windows)
  - `vibrancy: 'fullscreen-ui'` (для macOS)
  - `backgroundColor: '#00000000'` (полная прозрачность)
- Отключение аппаратного ускорения (`app.disableHardwareAcceleration()`) для фикса проблем с прозрачностью на Windows

**Ключевые решения:**
- Отключение `backgroundMaterial: 'acrylic'` — использование чистого CSS blur вместо нативного API Windows
- Использование `nodeIntegration: true` для доступа к Node.js API из рендерера

#### **index.html** — Структура UI
**Ответственность:**
- Разметка 5 каналов управления
- Подключение иконок Material Icons Round
- Drag-region для перемещения окна

**Структура канала:**
```html
<div class="channel">
  <div class="header">
    <div class="header-top">
      [иконка] [название] [настройки]
    </div>
    <div class="preset-pill">[пресет]</div>
    <div class="device-info">[устройство] [громкость]</div>
  </div>
  <div class="slider-container">
    <input type="range" class="vertical-slider">
  </div>
  <div class="controls-area">
    <button class="mute-icon-btn">...</button>
    <div class="apps-label">Приложения</div>
    <div class="app-box"></div>
  </div>
</div>
```

#### **styles.css** — Визуальный слой
**Ответственность:**
- **Glassmorphism эффект:**
  - `backdrop-filter: blur(20px)` — эффект размытия
  - Полупрозрачные градиенты (`rgba`)
  - Noise texture overlay для реалистичности
  - Borders с градиентной прозрачностью
- **UI Components:**
  - Вертикальные слайдеры (с ротацией на -90deg)
  - Кастомные thumbs с glow эффектом
  - Hover effects
  - Цветные preset pills

#### **renderer.js** — Интерактивность
**Ответственность:**
- Обработка событий слайдеров (обновление % громкости)
- Переключение состояния мута (иконка + цвет)
- Будущая интеграция с Audio API

**Паттерн:**
```javascript
function setupChannel(channel) {
  const slider = channel.querySelector('.vertical-slider');
  const volDisplay = channel.querySelector('.device-vol');
  const muteBtn = channel.querySelector('.mute-icon-btn');
  
  // Event listeners...
}
```

---

## 4. Технологии и зависимости

### 4.1. Технологический стек

| Категория | Технология | Версия | Назначение |
|-----------|-----------|--------|-----------|
| **Runtime** | Electron | ^28.0.0 | Desktop приложение |
| **Frontend** | HTML5 | - | Структура UI |
| **Styling** | CSS3 | - | Визуальное оформление |
| **Scripting** | Vanilla JavaScript | ES6+ | Логика и интерактивность |
| **Icons** | Material Icons Round | - | Иконки UI |
| **Fonts** | Segoe UI, Roboto | - | Типографика |

### 4.2. Зависимости проекта

**package.json:**
```json
{
  "name": "sonar-glass-widget",
  "version": "1.0.0",
  "main": "main.js",
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
```

**Установка:**
```powershell
py -m npm install
```

**Запуск:**
```powershell
npm start
```

### 4.3. Внешние ресурсы
- **Material Icons Round:** `https://fonts.googleapis.com/icon?family=Material+Icons+Round`

---

## 5. Структура проекта

```
StellGG/
│
├── main.js                    # Main process Electron
├── index.html                 # UI разметка
├── renderer.js                # Renderer process логика
├── styles.css                 # Стили и glassmorphism
│
├── package.json               # Конфигурация npm
├── package-lock.json          # Lockfile зависимостей
│
├── Changelog.md               # История изменений
├── PROJECT_DOCUMENTATION.md   # Этот файл
│
└── node_modules/              # Зависимости (не включать в Git)
```

### Рекомендации по файловой структуре:
- ✅ Все файлы проекта в корне
- ✅ Простая flat структура для малых проектов
- 🔄 При масштабировании — создать папки `/src`, `/assets`, `/config`

---

## 6. Код-стайл и стандарты

### 6.1. Общие правила

#### Принцип DRY (Don't Repeat Yourself)
- ❌ **Не дублируй** код и логику
- ✅ **Выноси** повторяющиеся фрагменты в функции
- ✅ **Переиспользуй** существующие компоненты

**Пример:**
```javascript
// ✅ Правильно: универсальная функция
channels.forEach(channel => setupChannel(channel));

// ❌ Неправильно: дублирование кода для каждого канала
setupChannel1();
setupChannel2();
setupChannel3();
```

#### NO FALLBACK / NO MOCKING POLICY
**Полностью запрещён фэйковый/заглушечный код!**

❌ **Запрещено:**
- `try-catch` заглушки без реальной обработки
- Фиктивные return значения
- Комментарии `"Not Implemented"`

✅ **Требуется:**
- Полноценная рабочая логика
- Реальная интеграция с API
- Graceful error handling

### 6.2. JavaScript стандарты

**Соглашения о наименовании:**
- `camelCase` для переменных и функций
- `PascalCase` для классов
- `UPPER_CASE` для констант
- Осмысленные имена (не `a`, `b`, `temp`)

**Комментарии:**
```javascript
// ✅ Хорошо: описывает "почему", а не "что"
// Disable hardware acceleration to fix transparency issues on Windows
app.disableHardwareAcceleration();

// ❌ Плохо: очевидное утверждение
// Set value to 50
const value = 50;
```

**Event Listeners:**
- Всегда проверяй наличие элементов перед добавлением слушателей:
```javascript
if (slider && volDisplay) {
  slider.addEventListener('input', (e) => { ... });
}
```

### 6.3. HTML стандарты

- ✅ Семантическая разметка
- ✅ Используй `class` вместо `id` для стилизации
- ✅ Логическая структура вложенности
- ✅ Русский язык для пользовательского текста

### 6.4. CSS стандарты

**Архитектура стилей:**
```css
/* 1. Global Resets */
body { ... }

/* 2. Layout Components */
.container { ... }

/* 3. UI Components */
.channel { ... }

/* 4. Interactive Elements */
input[type=range] { ... }
```

**Naming Convention:**
- `kebab-case` для классов
- Осмысленные имена (`.mute-icon-btn`, не `.btn1`)
- БЭМ-подобный подход: `.channel`, `.channel-header`, `.channel-slider`

**Glassmorphism Pattern:**
```css
.glass-element {
  background: linear-gradient(...);
  backdrop-filter: blur(20px);
  border: 1px solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
}
```

---

## 7. Правила ведения Changelog

### 7.1. Формат записи

**Обязательная структура:**

```markdown
## YYYY-MM-DD HH:MM
- [Краткое описание изменения]
- Причина: [почему это изменение было сделано]
- Автор: [кто внес изменение]
- Файлы: [список затронутых файлов]
```

### 7.2. Пример записи из текущего проекта

Из `Changelog.md` (строки 3-9):

```markdown
## 2025-11-24 22:50
- Усилен эффект размытия (Blur):
  - В `styles.css` увеличено значение `backdrop-filter` с 12px до 20px.
  - Немного увеличена непрозрачность градиента каналов для лучшей видимости эффекта "матового стекла".
- Причина: запрос пользователя ("чуть чуть блюра добавь").
- Автор: нейросеть (Antigravity).
- Файлы: styles.css.
```

### 7.3. Правила внесения записей

#### ✅ Обязательные действия:

1. **Перед каждым изменением:**
   - Открой и прочитай весь `Changelog.md`
   - Убедись, что не дублируешь предыдущие изменения
   - Проверь последнюю дату/время

2. **При внесении изменения:**
   - Добавь запись В НАЧАЛО файла (после заголовка `# Changelog`)
   - Используй точную дату и время (формат `YYYY-MM-DD HH:MM`)
   - Опиши ЧТО изменилось (кратко и ясно)
   - Укажи ПОЧЕМУ (контекст решения)
   - Перечисли все затронутые файлы

3. **После изменения:**
   - Убедись, что changelog актуален
   - Проверь синтаксис markdown
   - Сохрани файл

#### ❌ Запрещено:

- Пропускать записи об изменениях
- Изменять прошлые записи (только если не исправляешь ошибку)
- Использовать неструктурированный формат
- Оставлять поле "Причина" пустым
- Забывать указывать файлы

### 7.4. Типы изменений

Используй следующие префиксы для категоризации:

| Префикс | Тип изменения | Пример |
|---------|---------------|---------|
| **Добавлено:** | Новая функциональность | Добавлена функция автосохранения |
| **Исправлено:** | Багфикс | Исправлена проблема с прозрачностью |
| **Изменено:** | Модификация существующего | Изменен алгоритм расчета громкости |
| **Обновлено:** | Визуальное/стилевое обновление | Обновлен дизайн кнопок мута |
| **Удалено:** | Удаление функционала | Удален неиспользуемый код |
| **Оптимизировано:** | Улучшение производительности | Оптимизирован рендеринг слайдеров |

**Пример с префиксом:**
```markdown
## 2025-11-24 23:15
- **Добавлено:** Функция сохранения настроек громкости в localStorage.
- Причина: запрос пользователя — сохранять состояние между сеансами.
- Автор: нейросеть (Antigravity).
- Файлы: renderer.js, index.html.
```

---

## 8. Журнал ключевых изменений

### 8.1. Текущие записи Changelog

Полный changelog доступен в файле `Changelog.md`. Ключевые вехи:

**2025-11-24 22:50** — Усиление blur эффекта  
**2025-11-24 22:45** — Реализация интерактивности UI (слайдеры, мута)  
**2025-11-24 22:35** — Исправление проблемы с прозрачностью  
**2025-11-24 22:30** — Устранение "серого фона", улучшение glassmorphism  
**2025-11-24 22:25** — Обновление визуального стиля под референс

### 8.2. Исторические решения

#### Решение № 1: Отключение аппаратного ускорения
**Дата:** 2025-11-24 22:35  
**Проблема:** Прозрачность окна не работает на Windows  
**Решение:** `app.disableHardwareAcceleration()` в `main.js`  
**Причина:** Конфликт GPU рендеринга с прозрачными окнами в некоторых системах

#### Решение № 2: Отказ от `backgroundMaterial: 'acrylic'`
**Дата:** 2025-11-24 22:35  
**Проблема:** Конфликт нативного Acrylic эффекта Windows с CSS blur  
**Решение:** Использование чистого CSS `backdrop-filter`  
**Причина:** Больше контроля, кросс-платформенность

#### Решение № 3: Добавление noise texture
**Дата:** 2025-11-24 22:30  
**Проблема:** Эффект матового стекла выглядит "плоско"  
**Решение:** SVG noise overlay через `::after` псевдоэлемент  
**Причина:** Реалистичность, физическая достоверность эффекта

---

## 9. Best Practices

### 9.1. Разработка

#### При добавлении новых функций:
1. ✅ Проверь, не дублируется ли функционал
2. ✅ Создай unit-тесты (когда проект масштабируется)
3. ✅ Обнови документацию
4. ✅ Добавь запись в `Changelog.md`
5. ✅ Проверь на всех сценариях использования

#### При исправлении багов:
1. ✅ Воспроизведи баг локально
2. ✅ Определи root cause
3. ✅ Примени fix с минимальными изменениями
4. ✅ Проверь, не сломал ли fix другие части
5. ✅ Зафиксируй в changelog с пометкой "Исправлено:"

### 9.2. Работа с Git

**Commit messages:**
```
<type>: <краткое описание>

<детальное описание, если нужно>

Fixes: #<номер issue, если есть>
```

**Типы коммитов:**
- `feat:` — новая функция
- `fix:` — исправление бага
- `style:` — изменения стилей
- `refactor:` — рефакторинг без изменения функционала
- `docs:` — обновление документации
- `chore:` — обновление зависимостей, конфигов

**Пример:**
```
style: усилен эффект blur в glassmorphism

Увеличен backdrop-filter с 12px до 20px для более 
выраженного эффекта запотевшего стекла по запросу пользователя.

Refs: Changelog.md 2025-11-24 22:50
```

### 9.3. Testing

**Текущее состояние:**  
🔄 Unit-тесты пока не реализованы (проект на ранней стадии)

**Будущий plan:**
- Тестирование UI компонентов (Jest + Electron Testing)
- E2E тесты взаимодействия с Audio API
- Visual regression testing для glassmorphism эффектов

**Минимальная проверка перед релизом:**
- ✅ Все слайдеры работают
- ✅ Кнопки мута корректно переключаются
- ✅ Прозрачность отображается правильно
- ✅ Окно можно перетаскивать
- ✅ Нет console errors

### 9.4. Performance

**Glassmorphism оптимизация:**
- Используй `will-change: transform` для анимируемых элементов
- Избегай излишнего количества blur слоёв
- Ограничивай число одновременных `backdrop-filter` элементов

**JavaScript:**
- Используй `debounce` для частых событий (slide input)
- Минимизируй DOM queries (кешируй элементы)

---

## 10. Типовые ошибки и FAQ

### 10.1. Проблема: Прозрачность не работает

**Симптомы:**
- Окно имеет серый/белый фон вместо прозрачности
- `backdrop-filter` blur не применяется

**Решения:**
1. ✅ Проверь, что отключено аппаратное ускорение:
   ```javascript
   app.disableHardwareAcceleration()
   ```

2. ✅ Убедись, что `backgroundColor` установлен в прозрачный:
   ```javascript
   backgroundColor: '#00000000'
   ```

3. ✅ Удали `backgroundMaterial: 'acrylic'` если используется

4. ✅ В CSS убедись, что `body` имеет `background: transparent`

**Ссылка на changelog:** 2025-11-24 22:35

---

### 10.2. Проблема: Blur эффект недостаточно выражен

**Симптомы:**
- Стекло выглядит почти прозрачным без "матовости"

**Решения:**
1. ✅ Увеличь значение `backdrop-filter: blur()`:
   ```css
   backdrop-filter: blur(20px); /* было 12px */
   ```

2. ✅ Увеличь непрозрачность фонового градиента:
   ```css
   background: linear-gradient(180deg,
     rgba(0, 180, 255, 0.20) 0%,  /* было 0.12 */
     ...
   );
   ```

**Ссылка на changelog:** 2025-11-24 22:50

---

### 10.3. Проблема: Вертикальный слайдер не работает

**Симптомы:**
- Слайдер отображается горизонтально
- Или не реагирует на взаимодействие

**Решения:**
1. ✅ Проверь CSS transform:
   ```css
   transform: translate(-50%, -50%) rotate(-90deg);
   ```

2. ✅ Убедись, что атрибут `orient="vertical"` присутствует в HTML:
   ```html
   <input type="range" orient="vertical" class="vertical-slider">
   ```

3. ✅ Проверь z-index (slider должен быть выше track):
   ```css
   .vertical-slider { z-index: 3; }
   .slider-track-bg { z-index: 1; }
   ```

---

### 10.4. Проблема: Кнопка мута не меняет иконку

**Симптомы:**
- Клик на кнопку не переключает иконку
- Цвет не меняется на красный

**Решения:**
1. ✅ Убедись, что `renderer.js` подключен:
   ```html
   <script src="renderer.js"></script>
   ```

2. ✅ Проверь console на ошибки JavaScript

3. ✅ Убедись, что Material Icons загружены:
   ```html
   <link href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" rel="stylesheet">
   ```

**Ссылка на changelog:** 2025-11-24 22:45

---

### 10.5. FAQ: Как запустить проект?

**Шаги:**
```powershell
# 1. Установить зависимости (если еще не установлены)
py -m npm install

# 2. Запустить приложение
npm start
```

**Требования:**
- Node.js установлен
- npm доступен
- Windows 10+ (для лучшей поддержки прозрачности)

---

### 10.6. FAQ: Как изменить размер окна?

**Решение:**
В файле `main.js` измени параметры:
```javascript
const win = new BrowserWindow({
  width: 450,   // ← измени ширину
  height: 300,  // ← измени высоту
  // ...
});
```

**Рекомендации:**
- Пропорции ~3:2 (ширина:высота) оптимальны
- Минимальный размер: 400x250px
- Максимальный: 600x400px (чтобы не загромождать экран)

---

### 10.7. FAQ: Как добавить новый канал?

**Шаги:**
1. Скопируй структуру `.channel` в `index.html`
2. Измени:
   - Иконку (`.material-icons-round icon`)
   - Название (`.title`)
   - Цвет preset pill (класс `.preset-pill`)
3. CSS автоматически применится благодаря общим классам
4. Обнови `renderer.js` — функция `setupChannel()` подхватит новый канал автоматически

**Пример:**
```html
<div class="channel">
  <div class="header">
    <div class="header-top">
      <span class="material-icons-round icon">headphones</span>
      <span class="title">МУЗЫКА</span>
    </div>
    <div class="preset-pill pink">Custom</div>
    <!-- ... остальная структура ... -->
  </div>
</div>
```

---

## 11. Развитие и roadmap

### 11.1. Текущий статус (v1.0)

✅ **Реализовано:**
- Прозрачное окно с glassmorphism
- 5 каналов управления
- Интерактивные слайдеры
- Функционал мута
- Drag-and-drop окна

### 11.2. Ближайшие планы (v1.1)

🔄 **В разработке:**
- **Интеграция с Windows Audio API:**
  - Реальное управление громкостью системы
  - Привязка приложений к каналам
  - Отображение текущего уровня системной громкости

- **Сохранение настроек:**
  - localStorage для позиций слайдеров
  - Запоминание состояния мута
  - Восстановление при запуске

- **Context Menu:**
  - Настройки виджета (тема, размер)
  - Выход из приложения
  - О программе

### 11.3. Долгосрочные планы (v2.0+)

🚀 **Будущие возможности:**
- **Темы оформления:**
  - Light/Dark режимы
  - Пользовательские цветовые схемы
  - Альтернативные стили (neon, minimal, etc.)

- **Аудио-визуализация:**
  - Спектр в реальном времени
  - VU-meters для каждого канала

- **Профили:**
  - Пресеты для разных сценариев (Gaming, Work, Media)
  - Быстрое переключение

- **Hotkeys:**
  - Глобальные сочетания клавиш для управления
  - Customizable shortcuts

- **Auto-start:**
  - Запуск вместе с Windows
  - Минимизация в tray

### 11.4. Технический долг

⚠️ **Требует внимания:**
- [ ] Добавить unit-тесты
- [ ] Настроить CI/CD pipeline
- [ ] Оптимизировать рендеринг для слабых GPU
- [ ] Добавить error logging
- [ ] Создать installer (.exe)

---

## 12. Контакты и фидбэк

### 12.1. Разработка

**Текущий разработчик:** Нейросеть Antigravity  
**Проектный репозиторий:** _(указать ссылку на GitHub при наличии)_

### 12.2. Как оставить фидбэк

**Баги и предложения:**
- Создай issue в репозитории (если есть)
- Или опиши проблему в комментариях к коммиту

**Формат отчета о баге:**
```
**Описание:**
[краткое описание проблемы]

**Шаги воспроизведения:**
1. ...
2. ...

**Ожидаемое поведение:**
[что должно быть]

**Фактическое поведение:**
[что происходит]

**Окружение:**
- ОС: Windows 10/11
- Версия Electron: 28.0.0
- Версия приложения: 1.0.0
```

### 12.3. Contributing Guidelines

**Если хочешь внести вклад:**
1. ✅ Прочитай эту документацию полностью
2. ✅ Следуй правилам ведения Changelog
3. ✅ Соблюдай код-стайл
4. ✅ Тестируй изменения перед коммитом
5. ✅ Пиши осмысленные commit messages

---

## 📚 Дополнительные ресурсы

### Полезные ссылки:

- **Electron Documentation:** https://www.electronjs.org/docs
- **Material Icons:** https://fonts.google.com/icons
- **CSS Backdrop Filter:** https://developer.mozilla.org/en-US/docs/Web/CSS/backdrop-filter
- **Glassmorphism Generator:** https://css.glass/

### Рекомендуемое чтение:

- [ ] Electron Security Best Practices
- [ ] Windows Audio API Documentation
- [ ] CSS Performance Optimization

---

**Конец документации**  
Последнее обновление: 2025-11-24 22:56  
Версия: 1.0
